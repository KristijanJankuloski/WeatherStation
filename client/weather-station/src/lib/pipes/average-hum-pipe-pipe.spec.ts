import { AverageHumPipePipe } from './average-hum-pipe-pipe';

describe('AverageHumPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AverageHumPipePipe();
    expect(pipe).toBeTruthy();
  });
});
