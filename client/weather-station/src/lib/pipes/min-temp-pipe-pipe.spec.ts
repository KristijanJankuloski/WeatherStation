import { MinTempPipePipe } from './min-temp-pipe-pipe';

describe('MinTempPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MinTempPipePipe();
    expect(pipe).toBeTruthy();
  });
});
