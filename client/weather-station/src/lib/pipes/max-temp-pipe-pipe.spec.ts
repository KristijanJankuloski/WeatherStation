import { MaxTempPipePipe } from './max-temp-pipe-pipe';

describe('MaxTempPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MaxTempPipePipe();
    expect(pipe).toBeTruthy();
  });
});
